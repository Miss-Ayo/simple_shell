/**
 * main - main function
 * @ac: nomber of argument
 * @av: arguments
 * return (0) if successful
 */

#include "shell.h"

int main(int ac, char **av)
{
if (ac == 1)
shell(av[1]);
else
return (84);
return (0);
}
